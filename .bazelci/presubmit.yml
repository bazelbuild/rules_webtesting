---
platforms:
  ubuntu2204:
    shell_commands:
    - "sudo apt -y update && sudo apt -ylibasound2"
    - "sudo apt -y update && sudo apt -ylibatk-bridge2.0-0"
    - "sudo apt -y update && sudo apt -y  libatk1.0-0"
    - "sudo apt -y update && sudo apt -y  libc6"
    - "sudo apt -y update && sudo apt -y  libcairo2"
    - "sudo apt -y update && sudo apt -y  libcups2"
    - "sudo apt -y update && sudo apt -y  libdbus-1-3"
    - "sudo apt -y update && sudo apt -y  libexpat1"
    - "sudo apt -y update && sudo apt -y  libfontconfig1"
    - "sudo apt -y update && sudo apt -y  libgbm1"
    - "sudo apt -y update && sudo apt -y  libgcc1"
    - "sudo apt -y update && sudo apt -y  libglib2.0-0"
    - "sudo apt -y update && sudo apt -y  libgtk-3-0"
    - "sudo apt -y update && sudo apt -y  libnspr4"
    - "sudo apt -y update && sudo apt -y  libnss3"
    - "sudo apt -y update && sudo apt -y  libpango-1.0-0"
    - "sudo apt -y update && sudo apt -y  libpangocairo-1.0-0"
    - "sudo apt -y update && sudo apt -y  libstdc++6"
    - "sudo apt -y update && sudo apt -y  libx11-6"
    - "sudo apt -y update && sudo apt -y  libx11-xcb1"
    - "sudo apt -y update && sudo apt -y  libxcb1"
    - "sudo apt -y update && sudo apt -y  libxcomposite1"
    - "sudo apt -y update && sudo apt -y  libxcursor1"
    - "sudo apt -y update && sudo apt -y  libxdamage1"
    - "sudo apt -y update && sudo apt -y  libxext6"
    - "sudo apt -y update && sudo apt -y  libxfixes3"
    - "sudo apt -y update && sudo apt -y  libxi6"
    - "sudo apt -y update && sudo apt -y  libxrandr2"
    - "sudo apt -y update && sudo apt -y  libxrender1"
    - "sudo apt -y update && sudo apt -y  libxss1"
    - "sudo apt -y update && sudo apt -y  libxtst6"
    build_targets:
    - "//..."
    test_flags:
    - "--test_tag_filters=-noci,-sauce"
    test_targets:
    - "//..."
  macos:
    build_targets:
    - "//..."
    test_flags:
    - "--test_tag_filters=-noci,-sauce"
    test_targets:
    - "//..."
  windows:
    build_flags:
    - "--enable_runfiles"
    build_targets:
    - "//..."
    test_flags:
    - "--test_tag_filters=-noci,-sauce"
    - "--enable_runfiles"
    test_targets:
    - "//..."
