---
platforms:
  ubuntu2204:
    shell_commands:
    - "sudo apt -y update && sudo apt -y install libxcb1"
    build_targets:
    - "//..."
    test_flags:
    - "--test_tag_filters=-noci,-sauce"
    test_targets:
    - "//..."
  macos:
    build_targets:
    - "//..."
    test_flags:
    - "--test_tag_filters=-noci,-sauce"
    test_targets:
    - "//..."
  windows:
    build_flags:
    - "--enable_runfiles"
    build_targets:
    - "//..."
    test_flags:
    - "--test_tag_filters=-noci,-sauce"
    - "--enable_runfiles"
    test_targets:
    - "//..."
